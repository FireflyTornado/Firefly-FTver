# 用 openssl rand -hex 32 生成独立密钥；不要提交真实值。
PRESENCE_TOKEN=replace-with-a-long-random-secret

# 服务默认只监听本机回环地址，由 Nginx 对外代理。
PRESENCE_HOST=127.0.0.1
PRESENCE_PORT=8765

# presence 用户必须拥有此文件父目录的写权限。
PRESENCE_STATE_FILE=/var/lib/nightbug-presence/presence.json
